%% Import data from text file.
% Script for importing data from the following text file:
%
%    /Users/kunalmenda/Google Drive/2016 Spring/E160/E160/Jaguar_BaseCode_03_alt/bin/x86/Release/PWM_Zeigler_Kp2Ki0.txt
%
% To extend the code to different selected data or a different text file,
% generate a function instead of a script.

% Auto-generated by MATLAB on 2016/02/25 23:01:44

%% Initialize variables.
filename = '/Users/kunalmenda/Google Drive/2016 Spring/E160/E160/Jaguar_BaseCode_03_alt/bin/x86/Release/PWM_Zeigler_Kp6Ki3Kbeta8.txt';
delimiter = ' ';

%% Format string for each line of text:
%   column1: double (%f)
%	column2: double (%f)
%   column3: double (%f)
%	column4: double (%f)
%   column5: double (%f)
%	column6: double (%f)
%   column7: double (%f)
% For more information, see the TEXTSCAN documentation.
formatSpec = '%f%f%f%f%f%f%f%[^\n\r]';

%% Open the text file.
fileID = fopen(filename,'r');

%% Read columns of data according to format string.
% This call is based on the structure of the file used to generate this
% code. If an error occurs for a different file, try regenerating the code
% from the Import Tool.
dataArray = textscan(fileID, formatSpec, 'Delimiter', delimiter, 'MultipleDelimsAsOne', true,  'ReturnOnError', false);

%% Close the text file.
fclose(fileID);

%% Post processing for unimportable data.
% No unimportable data rules were applied during the import, so no post
% processing code is included. To generate code which works for
% unimportable data, select unimportable cells in a file and regenerate the
% script.

%% Allocate imported array to column variable names
time = dataArray{:, 1};
x = dataArray{:, 2};
y = dataArray{:, 3};
t = dataArray{:, 4};
desx = dataArray{:, 5};
desy = dataArray{:, 6};
dest = dataArray{:, 7};


%% Clear temporary variables
clearvars filename delimiter formatSpec fileID dataArray ans;

close all

hf = figure(2);
set(hf,'Units','Points');
set(hf,'Position',[650,550,350,300]);
set(hf,'PaperUnits','Points');
set(hf,'PaperPosition',[650,550,350,300]);

h = quiver([2,0],[2,0],[1,1],[0,0],0.2);
set(h,'LineWidth',2);
hold on
plot(x,y);
subset = 1:80:length(time);
h = quiver(x(subset),y(subset),cos(t(subset)),sin(t(subset)),0.5);
set(h,'LineWidth',2);
grid on
xlabel 'x [m]'
ylabel 'y [m]'

axis([-.5,2.5,-.5,2.5])
legend 'Desired States' 'True Trajectory' 'True \theta'  'Location' 'NorthWest'

title 'K_{p,PWM} = 6, K_{i,PWM} = 3, K_{beta} = 0.16';

print -dpng -r500 './PWMPlots/Kp6Ki3Kbeta16.png'

